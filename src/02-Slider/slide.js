const { __ } = wp.i18n;
const { InnerBlocks } = wp.blockEditor;
const { Fragment } = wp.element;



let renderSlider = (props) => {
  console.log(props)
  return (
    
    <Fragment>
      <InnerBlocks  />
    </Fragment>
  );
};

export default renderSlider;
